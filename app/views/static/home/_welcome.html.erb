<section id="welcome_text">
  <%# Welcome text %>
  <div class="welcome-text">
    <h1 class="module-heading"><%== t 'home.welcome' %></h1>
    <p style="font-size:25px"><%== t 'home.subtitle' %></p>
  </div>

  <%= render partial: 'search/common/search_box', locals: { is_lg: true } if TeSS::Config.site.dig('home_page', 'search_box') %>

</section>