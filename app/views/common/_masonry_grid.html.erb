<% horizontal ||= false %>

<% if !objects.blank? && objects.count > 0 %>
    <ul class="<% horizontal ? "masonry-horizontal" : "masonry media-grid" %>">
      <% objects.each do |object| %>
          <%= render object %>
      <% end %>
    </ul>
<% end %>

